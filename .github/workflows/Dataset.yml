name: Consolidation Football Data

on:
  schedule:
    - cron: '0 5 * * *'  # Tous les jours à 05:00 UTC
  workflow_dispatch:      # Permet de lancer manuellement le workflow

jobs:
  consolidate-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: 🔄 Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.12

      - name: Install dependencies
        # Si tu n’as pas requirements.txt, ignore cette étape ou ajoute pip install manuellement
        run: |
          pip install -U pip

      - name: Run consolidation script
        run: |
          echo "🔄 Début du script data.py"
          python data.py
          echo "✅ Script terminé"

      - name: Commit and push updated data
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Analysesdata.json
          git commit -m "Mise à jour automatique des données consolidées" || echo "ℹ️ Rien à commit"
          git push